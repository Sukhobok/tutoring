<div class="ss-container">
	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		My Profile
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		Notifications &amp; Security
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		Invite Friends
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		My Wallet
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		File Manager
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		Tutor Center
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		Financial Center
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		Groups Management
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		Classrooms Management
	</div>

	<div class="ss-section left-sidebar-section ss-link" data-ss-link="{{ URL::route('settings.profile') }}">
		Identity verification
	</div>
</div>